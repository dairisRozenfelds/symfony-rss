{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block content %}
    <Register
        submit-route="{{ submitRoute }}"
        validate-email-route="{{ validateEmailRoute }}"
        submit-redirect-route="{{ submitRedirectRoute }}"
        csrf-token="{{ csrf_token(tokenName) }}"></Register>
{% endblock %}
