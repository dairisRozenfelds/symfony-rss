{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block content %}
    <Login
        submit-route="{{ submitRoute }}"
        submit-redirect-route="{{ submitRedirectRoute }}"
        csrf-token="{{ csrf_token(tokenName) }}"
        register-route="{{ registerRoute }}"></Login>
{% endblock %}
