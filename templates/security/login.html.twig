{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block content %}
    <Login
        submit-route="{{ loginSubmitRoute }}"
        csrf-token="{{ csrf_token('authenticate') }}"
        register-route="{{ registerRoute }}"></Login>
{% endblock %}
